# Composants et Props

Les composants sont les briques de React.

## Créer un composant

```jsx
function Welcome(props) {
  return <h1>Bonjour, {props.name}</h1>;
}
```

## Les Props

Les props permettent de passer des données d'un composant parent à un composant enfant.
