# Introduction au HTML

## Qu'est-ce que le HTML ?

**HTML** (HyperText Markup Language) est le langage standard pour créer des pages web. Il décrit la **structure** du contenu d'une page.

### 🎯 Objectifs de ce chapitre

- Comprendre ce qu'est le HTML
- Connaître l'histoire du web
- Créer votre première page HTML

---

## L'histoire du Web

Le World Wide Web a été inventé par **Tim Berners-Lee** en 1989 au CERN. HTML est l'un des trois piliers fondamentaux du web :

| Technologie | Rôle |
|-------------|------|
| **HTML** | Structure du contenu |
| **CSS** | Présentation visuelle |
| **JavaScript** | Interactivité |

---

## Votre première page HTML

Voici le code minimal pour créer une page HTML :

```html
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Ma première page</title>
</head>
<body>
    <h1>Bonjour le monde !</h1>
    <p>Ceci est ma première page web.</p>
</body>
</html>
```

### 🔍 Explications

- `<!DOCTYPE html>` : Déclare que c'est un document HTML5
- `<html>` : Élément racine de la page
- `<head>` : Contient les métadonnées (titre, encodage, etc.)
- `<body>` : Contient le contenu visible de la page

---

## 💡 Mini-activité

Essayez de modifier le code ci-dessus pour :
1. Changer le titre de la page
2. Ajouter un deuxième paragraphe
3. Ajouter un sous-titre avec `<h2>`

---

## Points clés à retenir

✅ HTML structure le contenu des pages web  
✅ Les balises fonctionnent généralement par paires (ouvrante/fermante)  
✅ `<!DOCTYPE html>` définit le type de document  
✅ Le `<head>` contient les métadonnées, le `<body>` le contenu visible
